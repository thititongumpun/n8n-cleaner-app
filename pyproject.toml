[project]
name = "n8n-cleaner-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apscheduler>=3.11.2",
    "fastapi[standard]>=0.119.1",
    "ffmpeg-python>=0.2.0",
]
